<% if comments.size > 0 %>

  <ul class="comments">
    <% comments.each do |comment| %>
      <%= render :partial => 'comments/comment', :locals => { :comment => comment } %>
    <% end %>
  </ul>

<% else %>

  <p>No comments, you could be the first (wooooo)!</p>

<% end %>
